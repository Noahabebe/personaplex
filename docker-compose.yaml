services:
  personaplex:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8998:8998"
    environment:
      - NO_TORCH_COMPILE=1
      - CUDA_VISIBLE_DEVICES=""   # disables GPU usage
    env_file:
      - .env
    volumes:
      - ./.cache:/root/.cache
